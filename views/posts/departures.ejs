<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="css/my.css">
    <title>Airport Departures</title>
</head>
<body>  
  <table id="airportDepartures" class="tablecolor" style="width:100%" border="2px">
    <tr class="header">
      <th>Time</th>
      <th>Flight Number</th> 
      <th>Depart to</th>
      <th></th>
    </tr>
    <% let data =  departureDetails.flightTracks;  %>
    <% for (var i in data) { %>
      <tr>
        <td><p> Date: <%= data[i].departureDate.dateLocal.substr(0,10); %> </p> 
         <p> Time: <%= data[i].departureDate.dateLocal.substr(11,19); %> </p>
        </td>
        <td><%= data[i].carrierFsCode %> <%= data[i].flightNumber %></td>
        <td><%= data[i].arrivalAirportFsCode %> </td>
        <td> <a name="aiportCode" href="#" data-id=<%= data[i].flightId %>>  Flight details </a> </td>
      </tr>
    <% } %>        
    </table>
  </body>
</html>